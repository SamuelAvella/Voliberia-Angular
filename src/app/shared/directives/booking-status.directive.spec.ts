import { BookingStatusDirective } from './booking-status.directive';

describe('BookingStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new BookingStatusDirective();
    expect(directive).toBeTruthy();
  });
});
